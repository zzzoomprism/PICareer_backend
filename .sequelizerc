const path = require('path');

module.exports = {
  config: path.resolve('build-migrations', 'config.json'),
  'models-path': path.resolve('build-migrations', 'models'),
  'seeders-path': path.resolve('build-migrations', 'seeders'),
  'migrations-path': path.resolve('build-migrations', 'migrations')
};
